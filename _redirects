/home/                /

# /members/* and the policy/risk/qip/newsletter PDFs are protected by the
# netlify/edge-functions/members-gate.js Edge Function, not by a Role=
# redirect here — Netlify's `Role=` condition only works on Enterprise plans.
